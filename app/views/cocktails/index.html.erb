

  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)),
           url('<%= image_path("fondecran.jpg")%>')" >
  <div class="container" >
    <h1>  <strong> Amazing Cocktails by El Chaboche</strong></h1>
    <p>Change your life and learn to make cocktails</p>
    <a class="btn btn-success" href="#">Apply now</a>
  </div>


</div>
<div class="container">

  <div class="row">
     <%= link_to "Send letter", letter_path  %>
    <% @cocktails.each do |cocktail| %>
      <div class="col-xs-12 col-md-6 col-lg-4">
      <% if cocktail.photo.present? %>
        <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)),
         url('<%= cl_image_path cocktail.photo, height: 300, width: 400, crop: :fill %>')" >
          <%= link_to cocktail.name, cocktail_path(cocktail.id)  %>
        </div>
      <% else %>
          <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)),
           url('<%= image_path("moscou.jpg")%>')">
            <%= link_to cocktail.name, cocktail_path(cocktail.id) , class:"cokname" %>
          </div>
      <% end %>
      </div>
    <% end %>
    <div class="col-xs-12 col-md-6 col-lg-4">
      <%= link_to "Add a new one" , new_cocktail_path , class:"card" , id:"lastcard"%>
    </div>
  </div>
</div>


